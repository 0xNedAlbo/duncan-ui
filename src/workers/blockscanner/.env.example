# Blockscanner Worker - Environment Configuration
#
# This worker scans blockchain events from Uniswap V3 NonfungiblePositionManager
# and maintains a reorg-resistant event ledger for position tracking.

# ============================================================================
# Database Configuration (Required)
# ============================================================================
# PostgreSQL connection string (same database as main application)
DATABASE_URL="postgresql://midcurve:dev123@localhost:5432/midcurve_dev"

# Direct connection URL for Prisma (bypasses connection pooling)
DATABASE_URL_UNPOOLED="postgresql://midcurve:dev123@localhost:5432/midcurve_dev"

# ============================================================================
# Chain RPC Endpoints (Required)
# ============================================================================
# RPC URLs for blockchain data fetching
# Use high-quality RPC providers (Alchemy, Infura, QuickNode) for reliability
# Note: These are backend-only URLs (no NEXT_PUBLIC_ prefix)

ETHEREUM_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
ARBITRUM_RPC_URL=https://arb-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# ============================================================================
# Scanning Configuration (Optional)
# ============================================================================
# SCAN_CHAINS: Comma-separated list of chains to monitor
# Defaults to all supported chains: ethereum,arbitrum,base
# Example: Only scan Ethereum and Arbitrum
# SCAN_CHAINS=ethereum,arbitrum

# ============================================================================
# Logging Configuration (Optional)
# ============================================================================
# LOG_LEVEL: Controls log verbosity
# Options: silent, fatal, error, warn, info, debug, trace
# Default: info in production, debug in development
LOG_LEVEL=info

# ============================================================================
# Environment
# ============================================================================
# NODE_ENV: Application environment
# Options: development, production, test
# Default: development
NODE_ENV=production