# GraphQL CodeGen Configuration for Uniswap V3 Subgraph Types
# Note: We're using manually generated types in subgraph.generated.ts
# due to complexity of The Graph Protocol entity schemas

schema: "./src/schemas/uniswap-v3.graphql"
generates:
  # This configuration is kept for potential future use
  # Currently using manually generated types
  # src/types/subgraph.auto-generated.ts:
  #   plugins:
  #     - "typescript"
  #   config:
  #     scalars:
  #       BigInt: string
  #       BigDecimal: string  
  #       Bytes: string
  #       ID: string
  #     skipTypename: true
  #     esModules: true